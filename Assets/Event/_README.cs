/**
 * Exmaple: Events
 * 
 * The example consists of three GameObjects:
 * - a button invoking the event
 * - an event manager holding the event itself
 * - a text listening to the event
 * 
 * Setting up the GameObjects:
 * - Button:
 *   - add a script component: Event_Button.cs
 *   - in the Button component add an OnClick() event using the Button itself, calling Event_Button.OnButtonClick()
 * - Text:
 *   - add a script component: Event_Text.cs
 * - EventManager:
 *   - add a script component: Event_EventManager.cs
 * Everything else is done in code.
 * 
 */